
/*_*  I Can  &  I Will  *_*/

#include <iostream>
#include <string>
#include <algorithm>
#include <cmath>
#include <sstream>
#include <set>
#include <bitset>
#include <map>
#include <queue>
#include <deque>
#include <stack>
#include <vector>
#include <iomanip>
using namespace std;
void NasSaR()
{
	std::ios_base::sync_with_stdio(0);
	cin.tie(NULL);
	cout.tie(NULL);
}
int arr[100000];
int main()
{
	NasSaR();

	int n, q,a=1;
	while (cin >> n >> q && n&&q)
	{
		

		for (int i = 0; i < n; i++)
			cin >> arr[i];
		sort(arr, arr + n);
		cout << "CASE# " << a++ << ":" << "\n";
		while (q--)
		{
			int x, ans = -1, l = 0, r = n - 1, mid;
			cin >> x;
			while (l <= r)
			{			

				mid = (l + r) / 2;

				if (arr[mid] < x)
					l = mid + 1;
				else if (arr[mid] > x)
					r = mid - 1;
				else
				{
					ans = mid;
					r = mid - 1;
				}

			}

			if (ans != -1)
				cout << x << " found at " << ++ans << "\n";
			else
				cout << x << " not found\n";

		}

	}
	system("pause");
	return 0;
}
